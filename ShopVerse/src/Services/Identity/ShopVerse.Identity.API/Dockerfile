FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src
COPY . .
RUN dotnet restore "src/Services/Identity/ShopVerse.Identity.API/ShopVerse.Identity.API.csproj"
RUN dotnet publish "src/Services/Identity/ShopVerse.Identity.API/ShopVerse.Identity.API.csproj" -c Release -o /app/publish

FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS runtime
WORKDIR /app
EXPOSE 8080
COPY --from=build /app/publish .
ENTRYPOINT ["dotnet", "ShopVerse.Identity.API.dll"]
